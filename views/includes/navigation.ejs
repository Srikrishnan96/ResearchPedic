<nav class="bg-info py-2">
    <% if(loggedInUser){%>
        <span class='ml-4 text-white' style='font-size: 25px; float: left'>
            LABRAT&emsp;
        </span>
        <a class="btn text-white" href="/all-researches" style="text-decoration: none">All Posts</a>
            <a class="btn text-white" href="/user/my-surveys"> My Surveys</a>
    <% } else {%>
        <span class='ml-4 text-white' style='font-size: 25px'>
                LABRAT&emsp;
        </span>
    <% }%>
        <div style="float: right">
            <form action="/user/login" class="<%= loggedInUser?'disp-none' : 'disp-inline'%>" style="float: right">
                <button class='btn text-light mr-3'>
                        Login / Sign Up
                </button>
            </form>
        </div>
        <form action="/user/logout" class="<%= loggedInUser?'disp-inline' : 'disp-none'%>" method='POST'>
            <button class='btn text-white mr-3' style="float: right">
                    Logout
            </button>
        </form>
</nav>         